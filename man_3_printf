.\MANPAGE PRINTF.
.TH Man Version 3 "18 march 2020" "3.0" "_printf man page"

.SH NAME

.B _printf
- returns the number of characters printed and writes the output to stdout
.SH SYNOPSIS
_printf FORMAT [ARGUMENT]...

.B #include "holberton.h"
.B #include <stdio.h>
.B #include <stdarg.h>
.B #include <stdlib.h>
.B #include <unistd.h>

.B int _printf(const char *format, ...);

.SH DESCRIPTION

.B _printf() function give a output according to a format. this function
.B give a outpud stdout(standart output stream;

.SH RETURN VALUE

.B In a successful return, these functions return the number of characters printed (excluding
.B the null byte used to end output to strings).

.B If an output error is encountered, a negative value is returned.

.SH FORMAT OF THE FORMAT STRING

.B The format string is a character string, beginning and ending in its initial shift state, if any.
.B The format string is composed of zero or more directives: ordinary characters (not %)
.B which are copied unchanged to the output stream; and conversion specifications,
.B each of which results in fetching zero or more subsequent arguments.Each conversion specification 
.B is introduced by the character %, and ends with a conversion specifier.

.SH THE CONVERSION SPECIFIER

.B A character that specifies the type of conversion to be applied. The conversion specifiers and their 
.B meanings are:

.TP

.B c
.B the int argument is converted to an unsigned char, and the resulting character is written.

.B s
.B The const char * argument is expected to be a pointer to an array of character type (pointer to a string).

.B d, i
.B The int argument is converted to signed decimal notation.

.B %
.B A '%' is written. No argument is converted. The complete conversion specification is '%%'.

.B \\n
.B New line

.TP

.SH EXAMPLE

 " #include "holberton.h" "
.br
 " int main(void) "
 " _printf("%s\n", "HELLO, HOLBERTON"); "

.br

 " _printf("%d", 18); "

.br

 " } "

.br

.SH SEE INFORMATION IN:

.I printf(3)

.SH BUGS

.B No known bugs

.SH AUTHOR

.B Bryan Builes            --- 1482@holbertonschool.com
.B Valeria Alvarez Giraldo --- 1553@holbertonschool.com
.B Diego Alejandro Rojas   --- 1232@holbertonschool.com
